// Map dimensions
export const MAP_COLS = 60;
export const MAP_ROWS = 30;

// Hex rendering
export const HEX_SIZE = 12; // pixels, radius of each hex

// Game
export const CYCLE_DURATION_MS = 300_000; // 5 minutes
/** Multiplayer cycle length — change to 300_000 for 5-minute cycles. */
export const MULTIPLAYER_CYCLE_MS = 30_000;
export const MAX_CYCLES = 7;
export const STARTING_TEMPERATURE = 1.0; // °C above pre-industrial
